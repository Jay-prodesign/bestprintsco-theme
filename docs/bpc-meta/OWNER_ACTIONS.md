# Owner Action List

No credit-card or payment action is requested. Spend remains USD 0.

Case `1667557011016000` was automatically closed on 2026-08-12 without remediation. On 2026-08-15, Meta reconfirmed the root cause, rejected owner self-assignment of full control, and exposed the official `Business Manager admin dispute` recovery form. A live specialist could not be connected because of support volume.

## 1. Prepare and submit the Business Manager admin dispute evidence

- Platform: Meta Business Support Home.
- Exact screen: `Destek ekibiyle iletişime geç` -> issue type `Business Manager admin dispute`; the authenticated form is already open in Chrome.
- Why owner is required: Meta requires identity evidence, business-ownership evidence, a signed owner authorization, document upload, and final ownership affirmation. Codex cannot perform those owner acts.
- Required documents: (1) an accepted government ID for Gurkan Senel; (2) an unmodified document proving ownership of the business associated with the portfolio, such as an accepted registration/tax/bank/utility/trademark/domain-transfer document; and (3) a signed authorization letter on business letterhead stating the business contact details, requester's relationship, disputed portfolio ID if known, Facebook URL/user ID/email, reason for the request, current admin if known, and an ownership declaration.
- File requirements: unmodified, non-editable PDF, JPEG, or PNG. Do not send documents through chat or commit them to Git.
- Action: prepare the three documents and tell Codex `Belgeler hazır`. Codex will resume at the open form, draft the non-sensitive issue description and authorization-letter text from verified facts, then stop for the owner's private upload, signature/affirmation, and final Submit.
- Continuation trigger: `Belgeler hazır`, or Meta independently releases Instagram `17841401039012650`.

## 2. Optional secondary self-service release check

- Platform: Instagram mobile app, signed in to `@bestprintsco_`.
- Exact screen: Professional settings or linked Facebook Page settings; look only for `Release from Business Portfolio`, `Disconnect`, `Remove`, or `Switch` for `cvtie-ad` / `1104252943250335`.
- Why owner is required: any release control is available only in the owner's authenticated mobile session and may start a 30-day admin-response process.
- Action: use only an exact release/detach control. Do not add a card, delete an account, select Nstyled, or create a bypass asset.
- Continuation trigger: report the exact control/result or provide a screenshot.

## 3. Confirm the older `@bestprintsco_` iPhone session only if it is unfamiliar

- Platform: Instagram Accounts Center -> Password and security -> Where you're logged in -> `bestprintsco_`
- Exact owner decision: determine whether the older iPhone session shown in the current-account device list belongs to you.
- Why owner is required: only the owner can recognize a legitimate device/location.
- Action: if it is not yours, tell Codex `the older iPhone session is not mine`; Codex can then log out only that exact session. Other Instagram profiles were not active in the current Windows browser and were not logged out.
- Continuation trigger: owner confirms the session is trusted or explicitly identifies it as unauthorized.

## 4. Budget, payment, and final publish remain deliberately deferred

- Platform: Meta Ads Manager -> ad account `2258717414903571`
- Exact buttons: later `Add payment method` and `Publish`
- Why owner is required: financial details, exact budget approval, and live spend initiation are owner-only.
- Action now: none. When ready, approve a documented budget scenario, enter payment privately, and perform final Publish only after all launch gates pass.
- Continuation trigger: owner states the approved daily budget and confirms payment setup is complete.
